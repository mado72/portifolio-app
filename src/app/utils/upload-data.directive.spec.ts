import { UploadDataDirective } from './upload-data.directive';

describe('UploadDataDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadDataDirective();
    expect(directive).toBeTruthy();
  });
});
