import { CurrencyChoiceDirective } from './currency-choice.directive';

describe('CurrencyChoiceDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyChoiceDirective();
    expect(directive).toBeTruthy();
  });
});
