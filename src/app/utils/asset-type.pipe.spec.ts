import { AssetTypePipe } from './asset-type.pipe';

describe('AssetTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AssetTypePipe();
    expect(pipe).toBeTruthy();
  });
});
