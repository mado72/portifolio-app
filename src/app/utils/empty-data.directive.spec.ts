import { EmptyDataDirective } from './empty-data.directive';

describe('EmptyDataDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyDataDirective();
    expect(directive).toBeTruthy();
  });
});
