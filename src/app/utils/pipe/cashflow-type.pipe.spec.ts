import { CashflowTypePipe } from './cashflow-type.pipe';

describe('CashflowTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CashflowTypePipe();
    expect(pipe).toBeTruthy();
  });
});
