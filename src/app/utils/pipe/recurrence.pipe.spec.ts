import { RecurrencePipe } from './recurrence.pipe';

describe('RecurrencePipe', () => {
  it('create an instance', () => {
    const pipe = new RecurrencePipe();
    expect(pipe).toBeTruthy();
  });
});
